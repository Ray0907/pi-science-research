name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Test (${{ matrix.os }}, Node ${{ matrix.node }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            node: '22.19.0'
            suite: full
          - os: ubuntu-latest
            node: '24.x'
            suite: full
          - os: windows-latest
            node: '24.x'
            suite: acquisition
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ matrix.node }}
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        run: npm ci --ignore-scripts
      - name: Run full checks
        if: matrix.suite == 'full'
        run: npm run check -- --testTimeout=30000
      - name: Type-check Windows acquisition
        if: matrix.suite == 'acquisition'
        run: npm run typecheck
      - name: Run Windows acquisition checks
        if: matrix.suite == 'acquisition'
        run: npm test -- --run --testTimeout=30000 tests/acquisition tests/extensions/academic-tools.test.ts

  package:
    name: Package audit
    needs: test
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '24.x'
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        run: npm ci --ignore-scripts
      - name: Audit package contents
        shell: bash
        run: |
          npm pack --dry-run --json --ignore-scripts > "$RUNNER_TEMP/npm-pack.json"
          node --input-type=module <<'NODE'
          import { readFileSync } from "node:fs";
          import { join } from "node:path";

          const fail = () => { throw new Error("package audit rejected"); };
          const runnerTemp = process.env.RUNNER_TEMP;
          if (typeof runnerTemp !== "string" || runnerTemp.length === 0) fail();

          let root;
          try { root = JSON.parse(readFileSync(join(runnerTemp, "npm-pack.json"), "utf8")); }
          catch { fail(); }
          if (!Array.isArray(root) || root.length !== 1) fail();
          const entry = root[0];
          if (entry === null || typeof entry !== "object" || !Array.isArray(entry.files)) fail();

          const paths = entry.files.map((file) => {
            if (file === null || typeof file !== "object" || typeof file.path !== "string") fail();
            return file.path;
          });
          if (paths.length !== 57 || new Set(paths).size !== 57) fail();

          const required = [
            "README.md",
            "LICENSE",
            "package.json",
            "extensions/research/index.ts",
            "src/index.ts",
          ];
          if (required.some((path) => !paths.includes(path))) fail();

          const allowedRoots = new Set(["README.md", "LICENSE", "package.json"]);
          if (paths.some((path) => !allowedRoots.has(path) && !path.startsWith("extensions/") && !path.startsWith("src/"))) fail();
          if (paths.some((path) =>
            path.startsWith("docs/") ||
            path.startsWith("tests/") ||
            path.includes("/fixtures/") ||
            path === "tsconfig.json" ||
            path === "vitest.config.ts" ||
            path.endsWith(".tmp") ||
            path.includes(".original.")
          )) fail();

          console.log(`package-files=${paths.length}`);
          NODE
