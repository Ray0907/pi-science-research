import {createBrotliDecompress,createGunzip,createInflate} from "node:zlib";
import {types as utilTypes} from "node:util";

import {sha256Hex} from "../crypto/hash.js";
import {assertNormalizedAcquisitionOptionsInternal,type NormalizedAcquisitionOptionsInternal,type TransportFailureCode,type TransportSettlement} from "./contracts.js";
import {getProviderJsonSettlementInternal,ProviderJsonScanError,scanAndParseProviderJsonInternal,type ProviderJsonScanErrorCode,type ProviderJsonValueInternal} from "./json-wire-scanner-internal.js";
import {SecureTransportError,assertPinnedHopRuntimeInternal,createNodeRuntimeCapabilitiesInternal,createPinnedHopRuntimeInternal,type NodeRuntimeCapabilitiesInternal,type PinnedHopRuntimeInternal} from "./node-pinned-hop-internal.js";
import {RequestDeadlineErrorInternal,assertRequestDeadlineOwnedByInternal,assertRequestDeadlineSchedulerCapabilitiesInternal,createNodeRequestDeadlineSchedulerCapabilitiesInternal,type RequestDeadlineSchedulerCapabilitiesInternal} from "./request-deadline-internal.js";
import {createEncodedTransportInternal,type EncodedTransportInternal,type EncodedTransportOutcomeInternal,type EncodedTransportRequestHandleInternal,type EncodedTransportSuccessInternal,type SecureTransportRequest} from "./transport.js";

export interface ContentDecodeOperationsDescriptorInternal{readonly decodeContentLayer:(encoding:"gzip"|"deflate"|"br",input:Uint8Array,maxOutputBytes:number,decodedOutputCap:number,ratioOutputCap:number,signal:AbortSignal)=>Promise<Uint8Array>;}
export interface ContentDecodeCapabilitiesInternal{readonly capabilityKind:"content-decode-capabilities";}
export interface SecureTransportCapabilitiesInternal{readonly capabilityKind:"secure-transport-operations";}
export interface SecureJsonTransport{readonly capabilityKind:"secure-json-transport";request(input:SecureTransportRequest,signal?:AbortSignal):Promise<TransportSettlement>;close():Promise<void>;}
export type PhysicalRequestAbortReasonInternal="dispatch-invariant"|"invocation-cancelled"|"security-stop"|"client-close"|"shutdown";
export interface SecureTransportRequestHandleInternal{readonly capabilityKind:"secure-transport-request-handle";readonly completion:Promise<TransportSettlement>;abort(reason:PhysicalRequestAbortReasonInternal):void;close():Promise<void>;forceClose():void;}

type DecodeOperation=ContentDecodeOperationsDescriptorInternal["decodeContentLayer"];
type SecureCapabilitiesState={readonly pinned:PinnedHopRuntimeInternal;readonly decoder:ContentDecodeCapabilitiesInternal;readonly scheduler:RequestDeadlineSchedulerCapabilitiesInternal;consumed:boolean};
type SecureState={readonly options:NormalizedAcquisitionOptionsInternal;readonly encoded:EncodedTransportInternal;readonly decoder:ContentDecodeCapabilitiesInternal;open:boolean;readonly active:Set<SecureRequestState>;closePromise:Promise<void>|null;output:SecureJsonTransport};
type SecureRequestState={readonly transport:SecureState;readonly encodedHandle:EncodedTransportRequestHandleInternal;gate:boolean;settled:boolean;completion:Promise<TransportSettlement>;resolve:(settlement:TransportSettlement)=>void;cleanup:Promise<void>;};
class DecodeFailure extends Error{readonly code:TransportFailureCode;constructor(code:TransportFailureCode){super(`Content decode rejected (${code})`);this.name="DecodeFailure";this.code=code;}}
const decoderStates=new WeakMap<object,DecodeOperation>();const secureCapabilityStates=new WeakMap<object,SecureCapabilitiesState>();const secureStates=new WeakMap<object,SecureState>();const secureRequestStates=new WeakMap<object,SecureRequestState>();const usedPinned=new WeakSet<object>();const usedDecoders=new WeakSet<object>();const usedSchedulers=new WeakSet<object>();
const ABORT_REASONS=new Set<PhysicalRequestAbortReasonInternal>(["dispatch-invariant","invocation-cancelled","security-stop","client-close","shutdown"]);
const ADD_EVENT=EventTarget.prototype.addEventListener;const REMOVE_EVENT=EventTarget.prototype.removeEventListener;const ABORTED_GETTER=Object.getOwnPropertyDescriptor(AbortSignal.prototype,"aborted")!.get!;
const TYPED_ARRAY_BYTE_LENGTH_GETTER=Object.getOwnPropertyDescriptor(Object.getPrototypeOf(Uint8Array.prototype) as object,"byteLength")!.get!;
function fail(code:"transport.invalid-options"|"transport.invalid-input"|"transport.invalid-capability"|"transport.unsupported-runtime"|"transport.closed"):never{throw new SecureTransportError(code);}
function typedLength(value:unknown):number|null{if(value===null||typeof value!=="object"||utilTypes.isProxy(value)||Object.getPrototypeOf(value)!==Uint8Array.prototype)return null;try{const length=TYPED_ARRAY_BYTE_LENGTH_GETTER.call(value) as number;return Number.isSafeInteger(length)&&length>=0?length:null;}catch{return null;}}
function exactDescriptor(value:unknown):DecodeOperation{if(value===null||typeof value!=="object"||utilTypes.isProxy(value)||Object.getPrototypeOf(value)!==Object.prototype)fail("transport.invalid-capability");const descriptors=Object.getOwnPropertyDescriptors(value);const keys=Reflect.ownKeys(descriptors);if(keys.length!==1||keys[0]!=="decodeContentLayer"){fail("transport.invalid-capability");}const descriptor=descriptors.decodeContentLayer;if(!descriptor||!("value" in descriptor)||!descriptor.enumerable||typeof descriptor.value!=="function")fail("transport.invalid-capability");return descriptor.value as DecodeOperation;}
function decoderOperation(value:unknown):DecodeOperation{if(value===null||typeof value!=="object"||utilTypes.isProxy(value)||!Object.isFrozen(value))fail("transport.invalid-capability");const operation=decoderStates.get(value);if(!operation)fail("transport.invalid-capability");return operation;}
export function createContentDecodeCapabilitiesInternal(descriptor:ContentDecodeOperationsDescriptorInternal):ContentDecodeCapabilitiesInternal{const operation=exactDescriptor(descriptor);const output=Object.freeze({capabilityKind:"content-decode-capabilities" as const});decoderStates.set(output,operation);return output;}
function checkedMultiply(left:number,right:number):number{const value=left*right;if(!Number.isSafeInteger(value)||value<0)throw new DecodeFailure("transport.decompression-ratio");return value;}
function checkedAdd(left:number,right:number,code:TransportFailureCode):number{const value=left+right;if(!Number.isSafeInteger(value)||value<left)throw new DecodeFailure(code);return value;}
function nodeDecodeContentLayer(encoding:"gzip"|"deflate"|"br",input:Uint8Array,maxOutputBytes:number,decodedOutputCap:number,ratioOutputCap:number,signal:AbortSignal):Promise<Uint8Array>{
  if(!Number.isSafeInteger(maxOutputBytes)||maxOutputBytes<0||!Number.isSafeInteger(decodedOutputCap)||decodedOutputCap<maxOutputBytes||!Number.isSafeInteger(ratioOutputCap)||ratioOutputCap<maxOutputBytes||typedLength(input)===null)fail("transport.invalid-capability");
  return new Promise<Uint8Array>((resolve,reject)=>{const transform=encoding==="gzip"?createGunzip():encoding==="deflate"?createInflate():createBrotliDecompress();let gate=true;let destroyed=false;let outputBytes=0;const chunks:Uint8Array[]=[];const destroy=()=>{if(destroyed)return;destroyed=true;try{transform.destroy();}catch{/* quarantine */}};const cleanup=()=>{REMOVE_EVENT.call(signal,"abort",onAbort);transform.removeListener("data",onData);transform.removeListener("end",onEnd);};const stop=(error:DecodeFailure)=>{if(!gate)return;gate=false;cleanup();destroy();for(const chunk of chunks)chunk.fill(0);chunks.length=0;reject(error);};const onAbort=()=>stop(new DecodeFailure("transport.cancelled"));const onData=(raw:Buffer)=>{if(!gate)return;if(!Buffer.isBuffer(raw)){stop(new DecodeFailure("transport.decompression-failed"));return;}const length=raw.byteLength;let projected:number;try{projected=checkedAdd(outputBytes,length,"transport.decoded-too-large");}catch{stop(new DecodeFailure("transport.decoded-too-large"));return;}const decodedBreach=projected>decodedOutputCap;const ratioBreach=projected>ratioOutputCap;if(decodedBreach){stop(new DecodeFailure("transport.decoded-too-large"));return;}if(ratioBreach){stop(new DecodeFailure("transport.decompression-ratio"));return;}if(projected>maxOutputBytes){stop(new DecodeFailure("transport.decompression-ratio"));return;}if(length>0)chunks.push(new Uint8Array(raw));outputBytes=projected;};const onEnd=()=>{if(!gate)return;gate=false;cleanup();const result=new Uint8Array(outputBytes);let offset=0;for(const chunk of chunks){result.set(chunk,offset);offset+=chunk.byteLength;chunk.fill(0);}chunks.length=0;resolve(result);};transform.on("error",()=>{if(gate)stop(new DecodeFailure("transport.decompression-failed"));});transform.on("data",onData);transform.on("end",onEnd);ADD_EVENT.call(signal,"abort",onAbort,{once:true});if(ABORTED_GETTER.call(signal)){onAbort();return;}try{transform.end(input);}catch{stop(new DecodeFailure("transport.decompression-failed"));}});
}
export function createNodeContentDecodeCapabilitiesInternal():ContentDecodeCapabilitiesInternal{return createContentDecodeCapabilitiesInternal({decodeContentLayer:nodeDecodeContentLayer});}
export function createSecureTransportCapabilitiesInternal(pinnedRuntime:PinnedHopRuntimeInternal,decoder:ContentDecodeCapabilitiesInternal,deadlineScheduler:RequestDeadlineSchedulerCapabilitiesInternal):SecureTransportCapabilitiesInternal{try{assertPinnedHopRuntimeInternal(pinnedRuntime);}catch{return fail("transport.invalid-capability");}decoderOperation(decoder);try{assertRequestDeadlineSchedulerCapabilitiesInternal(deadlineScheduler);}catch{return fail("transport.invalid-capability");}if(usedPinned.has(pinnedRuntime as object)||usedDecoders.has(decoder as object)||usedSchedulers.has(deadlineScheduler as object))fail("transport.invalid-capability");usedPinned.add(pinnedRuntime as object);usedDecoders.add(decoder as object);usedSchedulers.add(deadlineScheduler as object);const output=Object.freeze({capabilityKind:"secure-transport-operations" as const});secureCapabilityStates.set(output,{pinned:pinnedRuntime,decoder,scheduler:deadlineScheduler,consumed:false});return output;}
export function createNodeSecureTransportCapabilitiesInternal(runtime:NodeRuntimeCapabilitiesInternal=createNodeRuntimeCapabilitiesInternal()):SecureTransportCapabilitiesInternal{return createSecureTransportCapabilitiesInternal(createPinnedHopRuntimeInternal(undefined,runtime),createNodeContentDecodeCapabilitiesInternal(),createNodeRequestDeadlineSchedulerCapabilitiesInternal(runtime));}
function capabilityState(value:unknown):SecureCapabilitiesState{if(value===null||typeof value!=="object"||utilTypes.isProxy(value)||!Object.isFrozen(value))fail("transport.invalid-capability");const state=secureCapabilityStates.get(value);if(!state||state.consumed)fail("transport.invalid-capability");return state;}
function successBase(encoded:EncodedTransportSuccessInternal){return{schemaVersion:1 as const,partitionKey:encoded.request.partitionKey,partitionKeySha256:encoded.request.partitionKeySha256,provider:encoded.request.provider,operation:encoded.request.operation,endpointClass:encoded.request.endpointClass,normalizedOrigin:new URL(encoded.request.url).origin,requestedUrl:encoded.request.url,finalUrl:encoded.finalUrl,redirectWitnesses:encoded.redirectWitnesses,httpStatus:encoded.httpStatus,encodedBytes:encoded.encodedBytes,responseHeaders:encoded.responseHeaders,responseHeadersSha256:encoded.responseHeadersSha256,connectedPeer:encoded.connectedPeer,startedAt:encoded.startedAt,settledAt:encoded.settledAt};}
function failureSettlement(encoded:EncodedTransportSuccessInternal,code:TransportFailureCode,decodedBytes=0):TransportSettlement{const base=successBase(encoded);if(code==="transport.cancelled"||code==="transport.deadline-exceeded")return Object.freeze({...base,decodedBytes,responsePayloadSha256:null,outcome:"cancelled" as const,failureCode:code,payloadUtf8:null});return Object.freeze({...base,decodedBytes,responsePayloadSha256:null,outcome:"failure" as const,failureCode:code as Exclude<TransportFailureCode,"transport.cancelled"|"transport.deadline-exceeded">,payloadUtf8:null});}
function registerSidecar(settlement:TransportSettlement,value:ProviderJsonValueInternal|null,errorCode:ProviderJsonScanErrorCode|null):void{const register=(getProviderJsonSettlementInternal as unknown as {registerInternal:(settlement:TransportSettlement,lookup:Readonly<{value:ProviderJsonValueInternal|null;errorCode:ProviderJsonScanErrorCode|null}>)=>void}).registerInternal;register(settlement,Object.freeze({value,errorCode}));}
function mapDeadline(error:unknown,gate:boolean,deadline:EncodedTransportRequestHandleInternal["deadline"]):TransportFailureCode{try{deadline.throwIfExpired();}catch(deadlineStateError){if(deadlineStateError instanceof RequestDeadlineErrorInternal)return deadlineStateError.code==="request-deadline.expired"?"transport.deadline-exceeded":"transport.cancelled";}if(!gate)return"transport.cancelled";if(error instanceof RequestDeadlineErrorInternal)return error.code==="request-deadline.expired"?"transport.deadline-exceeded":"transport.cancelled";if(error instanceof DecodeFailure)return error.code;return"transport.decompression-failed";}
async function decodeAndScan(state:SecureRequestState,encoded:EncodedTransportSuccessInternal):Promise<TransportSettlement>{const deadline=state.encodedHandle.deadline;const options=state.transport.options.transport;const layers=encoded.contentEncodings;if(layers.length>options.maxEncodingLayers)return failureSettlement(encoded,"transport.too-many-encoding-layers");let bytes:Uint8Array;try{assertRequestDeadlineOwnedByInternal(deadline,state.encodedHandle.ownerId);deadline.throwIfExpired();bytes=state.transport.encoded.consumeBody(encoded);}catch(initialError){return failureSettlement(encoded,mapDeadline(initialError,state.gate,deadline));}const wholeDecodedCap=options.maxDecodedBodyBytes;const wholeRatioCap=checkedMultiply(options.maxDecompressionRatio,Math.max(1,encoded.encodedBytes));let cumulative=0;try{const decode=decoderOperation(state.transport.decoder);const reversed=[...layers].reverse();for(const layer of reversed){if(layer==="identity")continue;if(!state.gate)throw new DecodeFailure("transport.cancelled");deadline.throwIfExpired();const inputBytes=bytes.byteLength;const layerRatioCap=checkedMultiply(options.maxDecompressionRatio,Math.max(1,inputBytes));const decodedRemaining=wholeDecodedCap-cumulative;const ratioRemaining=Math.min(layerRatioCap,wholeRatioCap-cumulative);if(decodedRemaining<0)throw new DecodeFailure("transport.decoded-too-large");if(ratioRemaining<0)throw new DecodeFailure("transport.decompression-ratio");const maximum=Math.min(decodedRemaining,ratioRemaining);const prior=bytes;let output:Uint8Array;try{output=await decode(layer,prior,maximum,decodedRemaining,ratioRemaining,deadline.signal);}catch(decodeError){throw decodeError;}const length=typedLength(output);if(length===null)throw new DecodeFailure("transport.decompression-failed");if(!state.gate){output.fill(0);throw new DecodeFailure("transport.cancelled");}try{deadline.throwIfExpired();}catch(deadlineError){output.fill(0);throw deadlineError;}const decodedBreach=length>decodedRemaining;const ratioBreach=length>ratioRemaining;if(decodedBreach){output.fill(0);throw new DecodeFailure("transport.decoded-too-large");}if(ratioBreach){output.fill(0);throw new DecodeFailure("transport.decompression-ratio");}cumulative=checkedAdd(cumulative,length,"transport.decoded-too-large");if(cumulative>wholeDecodedCap)throw new DecodeFailure("transport.decoded-too-large");if(cumulative>wholeRatioCap)throw new DecodeFailure("transport.decompression-ratio");bytes=new Uint8Array(output);prior.fill(0);if(output!==prior)output.fill(0);}if(reversed.length===0||reversed.every((layer)=>layer==="identity")){cumulative=bytes.byteLength;if(cumulative>wholeDecodedCap)throw new DecodeFailure("transport.decoded-too-large");if(cumulative>wholeRatioCap)throw new DecodeFailure("transport.decompression-ratio");}deadline.throwIfExpired();let payloadUtf8:string;try{payloadUtf8=new TextDecoder("utf-8",{fatal:true}).decode(bytes);}catch{throw new DecodeFailure("transport.invalid-utf8");}const decodedBytes=bytes.byteLength;const payloadHash=sha256Hex(bytes);bytes.fill(0);let value:ProviderJsonValueInternal;try{value=scanAndParseProviderJsonInternal(payloadUtf8,{maxDepth:state.transport.options.providers.maxStructureDepth,maxNodes:state.transport.options.providers.maxStructureNodes,maxKeys:state.transport.options.providers.maxStructureKeys,maxObjectWireBytes:state.transport.options.providers.maxObjectWireBytes,maxScalarCanonicalBytes:state.transport.options.providers.maxStringCanonicalBytes,maxAggregateScalarBytes:state.transport.options.providers.maxScalarCanonicalBytes},deadline);}catch(scanError){if(scanError instanceof RequestDeadlineErrorInternal)throw scanError;const scanCode=scanError instanceof ProviderJsonScanError?scanError.code:"json.snapshot-mismatch";const code:TransportFailureCode=scanCode==="transport.json-root-invalid"?"transport.json-root-invalid":"transport.protocol-invalid";const settlement=failureSettlement(encoded,code,decodedBytes);registerSidecar(settlement,null,scanCode);return settlement;}deadline.throwIfExpired();const settlement=Object.freeze({...successBase(encoded),decodedBytes,responsePayloadSha256:payloadHash,outcome:"success" as const,failureCode:null,payloadUtf8});registerSidecar(settlement,value,null);return settlement;}catch(phaseError){bytes.fill(0);return failureSettlement(encoded,mapDeadline(phaseError,state.gate,deadline));}}
async function runRequest(state:SecureRequestState):Promise<void>{let settlement:TransportSettlement;try{const encodedOutcome=await state.encodedHandle.completion;if(encodedOutcome.outcome!=="encoded")settlement=encodedOutcome.settlement;else settlement=await decodeAndScan(state,encodedOutcome);state.settled=true;state.gate=false;}finally{state.cleanup=state.encodedHandle.close();await state.cleanup;state.transport.active.delete(state);}state.resolve(settlement);}
function openRequest(transport:SecureState,input:SecureTransportRequest,signal?:AbortSignal):SecureTransportRequestHandleInternal{if(!transport.open)fail("transport.closed");const encodedHandle=transport.encoded.openInternal(input,signal);encodedHandle.retainDeadlineForDecodeInternal();let resolve!:(settlement:TransportSettlement)=>void;const completion=new Promise<TransportSettlement>((done)=>{resolve=done;});const state:SecureRequestState={transport,encodedHandle,gate:true,settled:false,completion,resolve,cleanup:Promise.resolve()};let handle!:SecureTransportRequestHandleInternal;handle=Object.freeze({capabilityKind:"secure-transport-request-handle" as const,completion,abort(reason:PhysicalRequestAbortReasonInternal){if(!ABORT_REASONS.has(reason))fail("transport.invalid-input");const current=secureRequestStates.get(handle);if(current&&current.gate){current.gate=false;current.encodedHandle.abort();}},async close(){const current=secureRequestStates.get(handle);if(!current)return;if(current.gate){current.gate=false;current.encodedHandle.abort();}await current.completion;await current.cleanup;},forceClose(){const current=secureRequestStates.get(handle);if(current&&current.gate){current.gate=false;current.encodedHandle.forceClose();}}});secureRequestStates.set(handle,state);transport.active.add(state);void runRequest(state);return handle;}
export function createSecureJsonTransport(options:NormalizedAcquisitionOptionsInternal,capabilities?:SecureTransportCapabilitiesInternal):SecureJsonTransport{try{assertNormalizedAcquisitionOptionsInternal(options);}catch{return fail("transport.invalid-capability");}const actual=capabilities??createNodeSecureTransportCapabilitiesInternal();const cap=capabilityState(actual);cap.consumed=true;const encoded=createEncodedTransportInternal(options.transport,cap.pinned,cap.scheduler);let output!:SecureJsonTransport;const state:SecureState={options,encoded,decoder:cap.decoder,open:true,active:new Set(),closePromise:null,output:null as unknown as SecureJsonTransport};output=Object.freeze({capabilityKind:"secure-json-transport" as const,request(input:SecureTransportRequest,signal?:AbortSignal){return openRequest(state,input,signal).completion;},async close(){if(state.closePromise)return state.closePromise;state.open=false;state.closePromise=(async()=>{for(const request of [...state.active]){request.gate=false;request.encodedHandle.forceClose();}await Promise.all([...state.active].map((request)=>request.completion));await encoded.close();})();return state.closePromise;}});state.output=output;secureStates.set(output,state);return output;}
export function assertSecureJsonTransportInternal(value:unknown):asserts value is SecureJsonTransport{if(value===null||typeof value!=="object"||utilTypes.isProxy(value)||!Object.isFrozen(value)||!secureStates.has(value))fail("transport.invalid-capability");}
export function openSecureTransportRequestInternal(transport:SecureJsonTransport,input:SecureTransportRequest,signal?:AbortSignal):SecureTransportRequestHandleInternal{assertSecureJsonTransportInternal(transport);return openRequest(secureStates.get(transport)!,input,signal);}
